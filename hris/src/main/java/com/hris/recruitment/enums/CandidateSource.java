package com.hris.recruitment.enums;

/**
 * How a candidate entered the talent pool / how an application originated.
 */
public enum CandidateSource {
    REFERRAL,
    JOB_BOARD,
    LINKEDIN,
    AGENCY,
    DIRECT,
    INTERNAL,
    OTHER
}
