package com.hris.leave.ledger.entity;

public enum LeaveBalanceTransactionType {
    ACCRUAL,
    REQUEST_RESERVATION,
    REQUEST_APPROVAL_CONFIRMATION,
    REQUEST_REJECTION_RELEASE,
    MANUAL_ADJUSTMENT,
    CANCELLATION_RELEASE,
    EXPIRATION
}
